const section = window.location.pathname;
const navlink = document.querySelectorAll('header nav a');


forEach( link =>{

   if(link.href.includes(`$(section)`)){  links.classlist.add( 'active' );}
  
    
})